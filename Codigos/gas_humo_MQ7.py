import network
import machine
import time
from umqtt.simple import MQTTClient

# 🔹 Configuración WiFi
SSID = "JAQUELINE"
PASSWORD = "12345678"

# 🔹 Configuración MQTT
MQTT_BROKER = "192.168.137.196"  # IP del servidor MQTT
MQTT_TOPIC = "sensor/mq7"

# 🔹 Configuración del sensor MQ-7 (salida analógica)
PIN_MQ7 = 34  # Conectar la salida analógica del MQ-7 a este GPIO
adc = machine.ADC(machine.Pin(PIN_MQ7))
adc.atten(machine.ADC.ATTN_0DB)  # Configuración de rango de 0-3.3V
adc.width(machine.ADC.WIDTH_12BIT)  # Configura la resolución a 12 bits (0-4095)

# 🔹 Función para conectar WiFi
def conectar_wifi():
    wifi = network.WLAN(network.STA_IF)
    wifi.active(True)
    if not wifi.isconnected():
        print("🔄 Conectando a WiFi...")
        wifi.connect(SSID, PASSWORD)
        tiempo_inicio = time.time()
        while not wifi.isconnected():
            if time.time() - tiempo_inicio > 10:
                print("❌ No se pudo conectar a WiFi")
                return False
            time.sleep(1)
    print("✅ Conectado a WiFi:", wifi.ifconfig())
    return True

# 🔹 Conectar WiFi
if not conectar_wifi():
    raise Exception("⚠ Error al conectar WiFi")

# 🔹 Configuración de MQTT
try:
    client = MQTTClient("ESP32_MQ7", MQTT_BROKER)
    client.connect()
    print("✅ Conectado a MQTT")
except Exception as e:
    print("❌ Error conectando a MQTT:", e)
    raise

# 🔹 Función para leer el sensor MQ-7
def leer_sensor():
    # Leer el valor analógico del sensor MQ-7 (valor entre 0 y 4095)
    return adc.read()

# 🔹 Función para enviar datos por MQTT
def enviar_datos():
    valor = leer_sensor()
    client.publish(MQTT_TOPIC, str(valor))
    print(f"📤 Enviado - MQ-7 Valor: {valor}")

# 🔹 Bucle principal
while True:
    try:
        enviar_datos()  # Leer y enviar el valor del sensor
        time.sleep(5)  # Ajusta el tiempo de muestreo
    except Exception as e:
        print("❌ Error en el bucle:", e)
